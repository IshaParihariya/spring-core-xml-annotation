<?xml version="1.0" encoding="UTF-8"?>
<!-- It’s the XML declaration that defines the XML version and encoding, usually UTF-8.(upper one) -->

<!--XML Configuration Header-->
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:context="http://www.springframework.org/schema/context"
       xsi:schemaLocation="
           http://www.springframework.org/schema/beans
           http://www.springframework.org/schema/beans/spring-beans.xsd
           http://www.springframework.org/schema/context
           http://www.springframework.org/schema/context/spring-context.xsd">

    <!--to tell that using annotations-->
    <context:annotation-config/>

    <!--tells that all packages and classes in com.isha there we using annotations
    Go inside this package and automatically create beans for classes with annotations like @Component
    NOTE : component-scan automatically enables annotation processing, so annotation-config becomes optional.-->
    <context:component-scan base-package="com.isha"/>

</beans>